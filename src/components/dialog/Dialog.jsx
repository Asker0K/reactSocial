import React from 'react';
import classes from './Dialog.module.css';

const Dialogs = (props) => {
    return (
        <div className={classes.content}>Dialogs
        </div>
    );
};

export default Dialogs;